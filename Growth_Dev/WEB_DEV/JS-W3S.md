"today" i will be learnin/teachin ya about JS (Javascript)

it is effectively the true brain of a page. html is simply the *body*, and css the *clothes*, js is the brain and will of a page.

first thing to talk about "getElementById()" it is the core to *find* an element of page by... *id* something both html and css "lessons" (notes ive done) talk about.

specific example and whys are; document.getElementById("demon").innerHTML = "demon time";

what it does? finds an element with the id demon, and what such is to demon time.
specifically innerHTML likely is just a reference as literal to the "inner" of the page.. i.e the html file itself and x things.


JS accepts both double and single quotes.

in simple sakes, js can change both html and css content pretty easily if you learn how to. from changing text, images, content, colors.. and more.

although idk if it is and or would get confused but, Java and Javascript are unrelated and not similar quite at all.

JS can simply be inserted effectively anywhere within 
`` <script> ... </script `` 
whether in head, body, or after it. like everything (or well css) you can reference external sources; be it within project source (another file somewhere near), an url path, and such.

to source external is ``<script src=".."></script>``
quite simple as that, what the source is, is up to ye to figure,

there are four simple (or only?) methods to use JS.
1. innerHTML or innerText
2. document.write()
3. window.alert()
4. console.log
another quote is to just literally print the page; window.print

innerHTML is simple search html element by id, innerText is same except just text. thus difference is html if changing actual content and element around (img, video, such) and text if.. text

honestly dont get what document.write does persay.

for window.alert is to make a popup box ("alert"), it can also though be used without the window part i.e alert

console.log outputs to the browser console. used for like debugging or/to change something.

back to print.. maybe it can be called just print idk.



